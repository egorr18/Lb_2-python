try:
    celsius = float(input("Введіть температуру в градусах Цельсія: "))
    fahrenheit = celsius * 9/5 + 32
    print(f"Температура у градусах Фаренгейта: {fahrenheit:.2f}")
except ValueError:
    print("Помилка: введіть числове значення температури!")
